# if submit4:
#     if submit4 is not None:
#         emp_details=e_df[e_df['Emp_id'] == submit4]
#         emp_details=emp_details.rename(columns=column_mapping)
#         if not emp_details.empty:
#     # Convert the filtered DataFrame to JSON format
#             employee_details_json = emp_details.to_json(orient='records', indent=4)
#         else:
#             print(f"No employee found with ID {submit4}")
#         response1=get_gemini_response_Description(input_prompt4,employee_details_json)
#         st.subheader("the response is ")
#         st.write(response1)

#     else:
#         st.write("Insert the EMployee ID")



# if submit3:
#     if response1 is not None:
#         pdf_content=response1
#         response2=get_gemini_response(input_prompt3,pdf_content,input_text)
#         st.subheader("the response is ")
#         st.write(response2)

#     else:
#         st.write("Please upload the response")